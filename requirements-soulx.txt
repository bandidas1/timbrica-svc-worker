# SoulX-Singer SVC runtime deps. torch/torchaudio installed separately (cu121).
#
# This is the SLIM set: upstream requirements.txt additionally pins nemo_toolkit,
# sageattention, gradio, torchcodec, webrtcvad, nltk, g2p_en, g2pM, ToJyutping,
# praat-parselmouth, pyloudnorm and scikit_image — all for the lyric-transcription /
# MIDI / web-UI preprocess tooling that a conversion request never touches.
# Verified on a real GPU: none of them appear in sys.modules after the handler
# loads the model + RMVPE, and the handler runs green without them. Dropping
# nemo alone saves several GB of image, which is cold-start latency.
accelerate==1.11.0
beartype==0.22.9
einops==0.8.2
funasr==1.3.0
librosa==0.11.0
loralib==0.1.2
matplotlib==3.10.8
mido==1.3.3
ml_collections==1.1.0
numba==0.63.1
numpy<2.0.0
omegaconf==2.3.0
packaging==24.2
pretty_midi==0.2.11
pyworld==0.3.5
rotary_embedding_torch==0.8.9
scikit_learn==1.7.2
scipy==1.15.3
setuptools==81.0.0
six==1.17.0
soundfile==0.13.1
tqdm==4.67.1
transformers==4.41.2
wandb==0.24.2

# transformers 4.41.2 requires hub <1.0 (the `hf` CLI drags in >=1.0)
huggingface-hub>=0.23,<1.0

# provenance watermark + serverless runtime
resemble-perth
runpod>=1.7.0
